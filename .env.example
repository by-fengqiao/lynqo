# CONFIG_REQUIRED: VITE_LYNQO_API_BASE_URL
# Optional. Leave blank for the usual LAN pairing page: API requests then use
# the currently opened page origin (for example, the QR-code LAN address).
VITE_LYNQO_API_BASE_URL=
