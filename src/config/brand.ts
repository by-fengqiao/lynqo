/**
 * Single source of truth for the user-facing product name in the web UI.
 * Package metadata remains in package.json and tauri.conf.json because those
 * build tools require literal values.
 */
export const APP_NAME = "LanNook";
